# v1.1 实现日志

沿用 v1.0 记录粒度：每个阶段记录实现内容、验证结论、行为等价自查表、偏差、单测清单、构建结果。

---

## 预执行决策（用户裁决，抄录自开工指令，后续会话以此为准不再重复提问）

- **PD-1 可选项取舍（实施计划 §4.3）**：O-1 文件行能力badge=做；O-2 清理缓存容量显示=做；O-3 分享列表本地搜索=做；O-4 文件网格视图=不做；O-5 测试连接耗时显示=做。
- **PD-2** H0 文档生成后不暂停等待用户审阅，直接继续 H1（用户会事后自查，发现问题会插话纠正）。
- **PD-3** 允许自行执行 adb 安装/卸载/清理模拟器存储以腾挪磁盘；唯一例外：H5-T5 升级验证必须原地升级安装（`adb install -r -t`），不得卸载重装。
- **PD-4** 真机验证账号：沿用真实实例 `https://nas.siveci.com:18443` 的现有账号与 AVD 上的已有会话；若某项演示所需账号/权限不可用，按 v1.0 先例记入日志与 `KNOWN_ISSUES.md` 作为 caveat，不阻塞继续执行。
- **PD-5** H10 浅深色/小屏走查由执行者自行在 AVD 上完成并截图存档（保存到 `docs/plans/v1.1-walkthrough/`，文件名含"页面-模式"），需要用户人工确认观感的项汇总列在最终报告里，不中途暂停等待看图。

---

## H0 版本文档 + 基线核对

### H0-T1 / H0-T2 / H0-T3

生成 `v1.1_PRD.md`、`v1.1_EXECUTION_PLAN.md`；回写 `DECISION_LOG.md`（DEC-H1~H5 决议 + H0 基线核对修正说明）与 `VERSION_STATUS.md`（active=v1.1，target versionCode=7/versionName="1.1.0"）。均基于 `docs/plans/2026-07-07-home-workspace-ui-implementation-plan.md` 整理，未新增范围。

### H0-T4 基线核对结论（本轮实测，非转述）

| # | 核对项 | 方法 | 结论 | 对实施计划的影响 |
| --- | --- | --- | --- | --- |
| 1 | 文件列表排序/网格现状 | grep `FileListViewModel.kt` 全文 sort/grid/LazyVerticalGrid 相关符号 | 零匹配，当前无任何排序或视图切换状态 | 确认 L-4/O-4 是纯新增，ViewModel 内存态实现，不影响既有列表渲染路径 |
| 2 | 登出入口与方法名 | 读 `AuthRepository.kt` 接口全文 + grep `SessionManager` 全仓库引用点 | `AuthRepository` 无 `logout`/`invalidate`；`SessionManager.invalidate(instanceId)` 定义于 `core/auth`，消费方仅 `data/repository` 内部（如 `AuthRepositoryImpl` 401 处理），`feature:settings` 模块依赖链无法直接访问 `core:auth` | **修正实施计划 §7.3 表格**："AuthRepository 无新方法"的假设不成立；H6-T4 需新增 `AuthRepository.logout(instanceId)` 域接口方法（内部委托 `SessionManager.invalidate`）。判定：这是纯本地会话清除、零网络请求的新增方法，属 PRD §7.3 允许的例外，不触发实时决策流程 |
| 3 | 搜索结果 thumb 字段 | 读 `SearchDtos.kt` 全文 | `SearchNodeResp` 字段为 `parent/name/isDir/size/type`，无 thumb | 确认实施计划 §3.6 图05"结果行缩略图裁剪"结论成立，H9-T1 不做缩略图 |
| 4 | AdminStorage 容量字段 | 读 `AdminDtos.kt` 相关行 | `StorageDetails` 含 `TotalSpace`/`UsedSpace`（`DiskUsage` 内嵌对象，注释确认对应 Go `model.StorageDetails`） | 与实施计划"admin API 无容量数据"假设不符，但该展示项**不在 §4.3 封顶清单内**，按 PRD §4.2/§14 硬约束仍裁剪；H9-T5 管理台概览重排不展示存储容量数字，记入本表作为准确归因（非"无数据"，而是"有数据但不在本版本范围"） |
| 5 | 预览"浏览器打开"现状 | 读 `PreviewScreen.kt` 相关函数 | `openInBrowser()`（`ACTION_VIEW` 无 MIME）与 `openExternally()`（MIME 限定）均已存在 | H9-T4"打开方式"三瓦片（外部打开/下载/浏览器打开）直接复用这两个既有函数，无需新增分发逻辑 |

无一项触发 `AGENTS.md` 实时决策流程（均为确认现状或纯附加式修正），按 PD-2 不暂停，继续 H1。

### 构建结果

H0 仅涉及文档，无代码改动，未运行构建（符合阶段性质）。

### 下一阶段

H1 设计系统底座：`core:designsystem` 新增/扩展组件（`SectionHeader`/`QuickActionTile`/`GroupCard`/`EntryRow`/`HeroHeader`槽位/`InstanceSwitcherChip`/`AppNavigationBar`/`StatusSummaryStrip`/`CapabilityChips`/`OtpCodeInput`/`SegmentedSelector`/`SheetHeader`）+ `AdminEntryVisibility` 纯函数提取 + 任务摘要 reducer。参考图：02/04/06/09 局部。
