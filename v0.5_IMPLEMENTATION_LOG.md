# v0.5 实施日志

> 本文件按 Sprint 追加记录：完成任务、文件变更摘要、实现决定、偏差、V-50x 验证结论。体例配合 `v0.5_EXECUTION_PLAN.md` §11。

---

## S0：基线检查 + 后端源码获取 + 接口验证

### 环境基线
- `JAVA_HOME="C:/Program Files/Java/jdk-20" ./gradlew assembleDebug testDebugUnitTest`：**BUILD SUCCESSFUL**（27s，699 actionable tasks）。起点全绿，无需先修 v0.4 欠账再开工。
- 无 Android 真机/模拟器（`adb devices` 为空，沿用既有环境约束）。

### DEC-501 结论：重新获取后端源码（已完成）
`git clone --depth 1 https://github.com/OpenListTeam/OpenList.git` 成功（网络可用），已落地为 `openlist-ref/`（已在 `.gitignore:38`，未入暂存区）。覆盖面核实：`server/handles/{user,storage,driver,setting,index,task}.go`、`server/router.go`、`internal/model/{user,storage,setting}.go` 全部存在。**首次覆盖 admin handlers**（v0.4 checkout 未覆盖），§6 全部 10 项验证均以本次源码为一手依据，**非 provisional**。

### V-501 `/api/me` 管理员判定
`role` 为整型，`GENERAL=0/GUEST=1/ADMIN=2`（`internal/model/user.go:17-21`），`User.IsAdmin()`（`:78-80`）即 `role==2`。现有 `Session.isAdmin` 假设成立，零改动。`CurrentUser()`（`server/handles/auth.go:91-101`）密码字段已清空。**非 provisional**。

### V-509 `AuthAdmin` 中间件行为
非管理员调用 `/api/admin/*`：`middlewares/auth.go:142-150` 调 `common.ErrorStrResp(c, "You are not an admin", 403)` + `Abort()`。**HTTP 状态码固定 200**，语义状态在 body 的 `code` 字段（`server/common/common.go:95-105`，`Resp{Code,Message,Data}`）——与 v0.4 已确认的"统一状态码+JSON body"结论**不完全一致**：v0.4 结论是"HTTP 401/403 + JSON body"，v0.5 实测是"HTTP 200 + body.code=403"。**客户端结论**：判定管理员访问拒绝一律看 `body.code`，不看 HTTP 状态码；`ErrorMapping`/`safeApiCall` 需确认现有实现是否已按 `code` 字段判断（S1-T5 前核对，若现有实现假设 HTTP 状态码则需要防御式兼容两种形态）。**非 provisional**。

### V-502 `admin/user/list` / `admin/user/get`
- 路由：`GET /api/admin/user/list`（`page`/`per_page`）、`GET /api/admin/user/get?id=`。
- 信封：`{code,message,data:{content:[...],total}}`（`PageResp`）。
- **暴露字段**：`id/username/base_path/role/disabled/permission/sso_id/allow_ldap`（+ `password` 字段存在但值被清空）。
- **敏感字段确认零暴露**：`pwd_hash/salt/otp_secret/authn/pwd_ts` 均为 `json:"-"`，**根本不出现在响应 JSON 中**——比计划预期更安全，DTO 白名单只需按上述 9 字段声明，无需额外防御式跳过。**非 provisional**。

### S0-T5 日志响应体策略
后端自身无请求/响应体日志中间件；admin/user 响应不含敏感字段（同 V-502）。**客户端结论**：admin/user、admin/setting 响应体本身不含密码等硬敏感字段，但 `AdminSettingItem` 的 `value` 仍可能含 token/密钥类配置值（见 V-507），日志脱敏面收窄到"设置项 value"这一处，其余 admin 接口响应体无需额外禁用日志。

### V-503 `admin/storage/*`
- 路由：`GET /list`、`GET /get?id=`、`POST /enable?id=`、`POST /disable?id=`、`POST /load_all`（`server/router.go:137-145`）。
- Storage 字段：`mount_path/driver/disabled/order/remark/status/cache_expiration/custom_cache_policies/addition/disable_index/enable_sign/modified`（`internal/model/storage.go:8-24`）；`mount_details` 不在 Storage 结构体本身，而是 `StorageResp` 包装出的 `MountDetails *model.StorageDetails`（`server/handles/storage.go:23`），**3 秒超时、失败为 nil**（`:52-70`）。
- enable/disable：**同步**，成功 `SuccessResp` 空 body，失败 `ErrorResp(500)`。load_all：**异步**（`go func`，立即 200 返回，后台 goroutine 完成后发信号，`:180-210`）——**与计划假设一致**（PRD 允许"已提交/已完成"两种文案按此定稿为"已提交，重新加载正在后台进行"）。

### V-504 `admin/driver/*`
`GET /list`（`op.GetDriverInfoMap()`，map[string]interface{}）、`GET /names`（字符串数组）、`GET /info?driver=`（404 若未找到）。配置元数据为动态 map，**按计划兜底：只展示驱动名+关键摘要，不做强类型解析**。

### V-510 存储禁用后 fs/list 行为
`disable` 后 `storagesMap.Delete(mountPath)`（`internal/op/storage.go:214`）——存储从内存表彻底移除。`fs/list` 直接命中该路径：若无 virtualFiles 兜底则 **HTTP 200 + body.code=500，message 含 "failed get storage"**；若有同级 virtualFiles 则静默返回不含该存储的列表。**客户端结论**：缓存失效后不预测具体表现（500 或空列表两种都可能），只负责失效缓存+触发远程刷新，展示层统一走现有错误态渲染（P510 降级策略，与计划一致）。

### V-505 `admin/task/*` 七类
`SetupTaskRoute`（`server/handles/task.go:127-228`）为每个类型挂载 `GET /undone`、`GET /done`、`POST /info`（`tid` query）、`POST /cancel`、`POST /retry`、`POST /delete`。管理员视角：`isAdmin || uid==creator.ID` 判定（`:137-139`）——**管理员确认可见全部用户任务**。`retry`/`delete` handler 本身**不做状态前置校验**（无条件调用 `manager.Retry`/`manager.Remove`），校验落在 manager 层，失败以 body.message 透传。**客户端结论**：UI 按状态预禁用按钮仍是纯体验优化，不能替代后端校验；错误全部走 message 透传（与计划一致）。

### V-506 `admin/index/*`
`build`/`update`/`stop`/`clear`/`progress`（`server/handles/index.go`）。`update` 请求体 `UpdateIndexReq{Paths []string, MaxDepth int}`（无结构体级默认值，客户端按 DEC-504 传 `["/"]` + 合理默认深度，如 -1 或 0 视 v0.5 UI 需要）。`progress` 响应字段确认为 `ObjCount(uint64)/IsDone(bool)/LastDoneTime(*time.Time, 可空)/Error(string)`（`internal/model/search.go`）。`build`/`clear` 在运行中调用返回 "index is running" 错误。**非 provisional**。

### V-507 `admin/setting/*`
- **HTTP 方法与计划假设有出入**：`list`=GET、`get`=GET、`default`=**GET**（`server/router.go:152-158`），**PRD 表格中 `default` 为 POST 是错误的**，S1-T3 按源码 GET 实现，记入偏差。
- `group`（单个）/`groups`（逗号分隔）、`key`/`keys` 参数均支持。
- `flag` 常量：`PUBLIC=0/PRIVATE=1/READONLY=2/DEPRECATED=3`（`internal/model/setting.go:18-23`），**私密判定 = flag==1**，按 P-508 叠加 key 关键字兜底。
- Group 枚举：`SINGLE=0,SITE=1,STYLE=2,PREVIEW=3,GLOBAL=4,OFFLINE_DOWNLOAD=5,INDEX=6,SSO=7,LDAP=8,S3=9,FTP=10,TRAFFIC=11`。**非 provisional**。

### V-508 Web 管理台路径
`server/static/static.go:220` 确认前端路径为 **`/@manage`**（区分 `ManageHtml`/`IndexHtml` 两套构建产物）。**非 provisional**，`AdminWebFallbackRepository` 直接拼 `${baseUrl}/@manage`（+ 子路径部署前缀）。

### S0-T6 决策关闭
DEC-501（重新获取源码，已完成，覆盖 admin 面）/ DEC-502（webfallback 不升级）/ DEC-503（A 外部浏览器）/ DEC-504（A 默认 `/`）/ DEC-505（A 独立 Tab）/ DEC-506（A 沿用 caveat 口径，按用户 §2 修正：构建/单测类验收项本轮实测，真机项 caveat）—— 全部按用户开工指令关闭，不再等待确认。

### 与计划的偏差
1. ~~`admin/setting/default` 实际是 GET 非 POST（V-507）~~ **勘误（S1 复核）**：重新核对 `openlist-ref/server/router.go:157`（`setting.POST("/default", handles.DefaultSettings)`），实际是 **POST**，与 PRD §9.2 表格一致。本条 S0 结论记录有误，S1-T3 已按源码实现为 POST，此处更正存档，避免后续 Sprint 沿用错误结论。
2. V-509 揭示 admin 拒绝访问是 HTTP 200 + body.code=403（非独立 401/403 HTTP 状态码）——S1-T5 前需核对现有 `ErrorMapping`/`safeApiCall` 是否已支持此形态，必要时补充映射分支。
3. 敏感字段确认为服务端级别不暴露（比计划"防御式跳过"更强的保证），DTO 白名单实现更简单。

### 下一 Sprint 注意点
已核对 `core/network/ErrorMapping.kt`：`ApiResponse<T>.toApiResult()`/`safeApiCallUnit` 均**只看 `body.code`**（`code in 200..299` 才算成功），完全不依赖 HTTP 状态码，`codeToDomainError(403,...)` 已映射到 `DomainError.Forbidden`——与 V-509 结论天然兼容，**无需修改**，admin Repository 直接复用 `safeApiCall`/`safeApiCallUnit` 即可。`AdminAccessDenied` 可在 `AdminGateRepositoryImpl` 内把 403 场景的 `DomainError.Forbidden` 转译为专属子类（S1-T5）。

---

## S1：:feature:admin 模块 graduate + 基座

### S1-T1 模块创建
- `settings.gradle.kts` 新增 `:feature:admin`；`feature/admin/build.gradle.kts` 严格照抄 `feature/preview` 结构，依赖仅 `core:{common,model,domain,designsystem}`（清单写死在文件注释里，防止后续 Sprint 越界）。
- 删除 `app/src/main/java/io/openlist/client/feature/admin/Reserved.kt` 占位包（含目录）。
- `feature/webfallback/Reserved.kt` 占位包按 DEC-502 保留未动。
- `app/build.gradle.kts` 新增 `implementation(project(":feature:admin"))`。

### S1-T2 路由
- `Routes.kt` 新增 `ADMIN = "admin/{instanceId}?tab={tab}"` + `admin(instanceId, tab)` builder，风格与 `SEARCH`/`FILE_LIST` 的可选 query 参数一致。
- `feature/admin/AdminHostScreen.kt`：最小占位 Composable（接收 instanceId/tab/onBack，仅显示文字），真正的门控页 + `AdminScaffold`/`AdminTabRow` 在 S2-T3 实现。
- `OpenListNavHost.kt` 挂载 `Routes.ADMIN`，解码风格与 `PREVIEW`/`MEDIA_PLAYER` 一致。

### S1-T3 DTO + OpenListApi
- 新增 `core/network/dto/AdminDtos.kt`：`AdminUserDto`/`AdminUserPageDto`/`AdminStorageDto`/`AdminStorageDetailsDto`/`AdminStoragePageDto`/`AdminIndexProgressDto`/`AdminIndexUpdateReq`/`AdminSettingDto`，以及 `AdminDriverInfoMap` typealias（`Map<String, JsonElement>`，驱动信息结构动态不建固定 DTO）。任务组**未**新增 DTO，确认复用现有 `TaskInfoDto`（`core/network/dto/TaskDtos.kt`，字段与 `server/handles/task.go TaskInfo` 逐一核对一致）。
- 逐字段核对了 `openlist-ref` 真实源码（而非仅凭 S0 纪要转述）：`internal/model/user.go`（User 结构体，`PwdHash/PwdTS/Salt/OtpSecret/Authn` 均 `json:"-"`，`Password` 字段虽有 json tag 但读路径永远为空）、`internal/model/storage.go`（Storage/StorageDetails/DiskUsage）、`internal/model/setting.go`（SettingItem + Group/Flag 常量）、`internal/model/search.go`（IndexProgress）、`server/handles/{user,storage,driver,setting,index,task}.go`、`server/router.go`。
- `OpenListApi.kt` 新增全部 admin 方法：`adminUserList/Get`、`adminStorageList/Get/Enable/Disable/LoadAll`、`adminDriverList/Names/Info`、`adminTaskUndone/Done/Info/Cancel/Retry/Delete`（7 类任务复用同一组方法，`type` 作路径参数）、`adminIndexBuild/Update/Stop/Clear/Progress`、`adminSettingList/Get/Default`。

### S1-T3 对 S0 纪要的勘误（**重要**）
本轮直接重读 `openlist-ref/server/router.go:150-159` 源码（S0 纪要转述可能有误），确认：
- `setting.GET("/get")`、`setting.GET("/list")` —— 与 S0 纪要一致。
- `setting.POST("/default")` —— **S0 纪要第 61 行「default 实际是 GET」有误**，源码明确是 **POST**。已在上面"与计划的偏差"章节勘误存档；`OpenListApi.adminSettingDefault` 实现为 `@POST`。
- `admin/task/*` 的 `info/cancel/retry/delete` 确认为 **POST + `tid` query**（`server/handles/task.go` `taskRoute`），与计划一致。
- `AuthAdmin` 中间件确认 403 + `"You are not an admin"`（`server/middlewares/auth.go:142-150`），与 S0 V-509 一致。

### S1-T4 领域模型
`core/model/AdminModels.kt`：`AdminAccessState`、`AdminUserSummary`（无 password/pwdHash/salt/otpSecret，`otpEnabled` 标注 provisional——源码未找到确认的 2FA 状态字段）、`AdminUserPage`、`AdminStorageStatus`、`AdminStorageDetails`、`AdminStorageSummary`、`AdminStoragePage`、`AdminTask`（独立于 `RemoteTask`，不复用/不扩展）、`AdminIndexProgress`（`isRunning` 客户端推导）、`AdminSettingItem`（`isPrivate` 客户端推导）、`AdminOverview`、`AdminWebSection`、`WebFallbackTarget`。

### S1-T5 Repository 接口 + 空实现 + DI + DomainError
- `core/domain` 新增 7 个接口：`AdminGateRepository`/`AdminUserRepository`/`AdminStorageRepository`/`AdminTaskRepository`/`AdminIndexRepository`/`AdminSettingsRepository`/`AdminWebFallbackRepository`。
- `data/repository` 对应 7 个 `*Impl`，全部方法返回 `ApiResult.Failure(DomainError.Unknown(null))`（`AdminGateRepositoryImpl.observeAccess`/`AdminTaskRepositoryImpl.observeAdminTasks` 例外，返回 `flowOf` 占位值，因为返回类型是 `Flow` 不是 `ApiResult`）。
- `app/di/RepositoryModule.kt` 新增 7 个 `@Binds`。
- `core/common/DomainError.kt` 新增 `AdminAccessDenied`/`AdminApiUnavailable` 两个子类 + `toUserMessage` 文案，紧跟在 `ExternalOpenUnavailable` 之后，风格与既有子类一致。

### S1-T6 Room 8→9 + 组件映射审查
- 新增 `AdminCacheEntity`（`core/database/entity`）+ `AdminCacheDao`（基础 CRUD，TTL 判断留给 Repository 层，与 `PreviewCacheDao` 同一分工）。
- `OpenListDatabase` 版本 8→9，`entities` 数组追加 `AdminCacheEntity::class`。
- `Migrations.kt` 新增 `MIGRATION_8_9`（手写 SQL，`CREATE TABLE admin_cache` + 唯一索引 `(instanceId, scope, cacheKey)`），沿用 MIGRATION_7_8 的免责声明措辞（本环境仍无法跑 KSP 导出真实 schema，`9.json` 为手工推导，需在下次有完整构建环境时重新核对）。
- `app/di/DatabaseModule.kt` 注册 `MIGRATION_8_9` + `provideAdminCacheDao`。
- `InstanceRepositoryImpl.delete()` 追加 `adminCacheDao.deleteByInstanceId(id)`。
- 手工推导 `9.json`：复制 `8.json`，追加 `admin_cache` 表条目（字段/索引与 Entity/SQL 三方核对一致，见任务报告的核对表），version 字段改为 9，identityHash 占位符改用 `PLACEHOLDER_UNVERIFIED_9_...`。
- 组件映射审查（16 项）：详见 S1 任务完成报告；结论摘要——`AdminConfirmDialog`→直接复用 `ConfirmDialog`；`AdminListItem`→复用 `ListRowItem`（大部分场景）；`AdminStatusBadge`/`StorageStatusBadge`→复用 `StatusBadge`+`StatusTone`（可能需按需扩展 tone）；`AdminTaskCard`→复用 `TaskCard`（若需 creator 展示做薄封装）；`AdminTabRow`→新增（参照 `TaskTabRow` 模式，放 designsystem，7 Tab 需横向滚动而 `TaskTabRow` 当前是等宽不滚动，需要新变体）；其余 9 个新增组件（`AdminScaffold`/`AdminOverviewCard`/`AdminMetricRow`/`UserListItem`/`StorageListItem`/`IndexProgressPanel`/`SettingItemRow`/`WebFallbackCard`/`AdminActionSheet`/`AdminRefreshIndicator`）业务耦合度高，留在 `:feature:admin`；`AdminRefreshIndicator` 若只是 Material3 `PullToRefreshBox` 的薄封装可考虑不新增，直接在各列表页用标准写法。

### 验证
`JAVA_HOME="C:/Program Files/Java/jdk-20" ./gradlew :feature:admin:compileDebugKotlin :core:model:compileDebugKotlin :core:domain:compileDebugKotlin :core:network:compileDebugKotlin :data:repository:compileDebugKotlin :core:database:compileDebugKotlin :app:compileDebugKotlin` → **BUILD SUCCESSFUL**（304 actionable tasks）。`:core:database:testDebugUnitTest :data:repository:testDebugUnitTest` → **BUILD SUCCESSFUL**（`core:database` 测试目录本身 NO-SOURCE，非本轮引入的问题）。

### 遗留给 S2+
1. `AdminUserDto.otp`/`AdminUserSummary.otpEnabled` 的 2FA 状态字段未在源码确认，S3 用户详情页实现前需要再次搜索或用真实实例验证，暂时保守显示为"未知"而非直接显示 false。
2. `AdminIndexRepository.updateIndex` 的 `maxDepth` 默认值语义（DEC-504）仍待 S6 结合 UI 需要定夺（当前 `AdminIndexUpdateReq` 默认值为 0）。
3. designsystem 的 `TaskTabRow` 目前等宽不滚动，S2-T3 做 `AdminTabRow`（7 Tab）时需要新变体或改造，不能直接复用。

---

## S2：管理员门控 + 概览

### S2-T1 AdminGateRepositoryImpl + 单测
- `AdminGateRepositoryImpl`（`data/repository`）：`checkAccess` 先读本地 `AuthRepository.getSession`（区分"从未登录"vs"曾登录后失效"，避免两者都落到 `refreshCurrentUser` 内部的 `DomainError.Unauthorized` 短路而混淆），无 session/guest session 直接返回 `DENIED_GUEST`；否则调用 `AuthRepository.refreshCurrentUser` 强制刷新 `/api/me`，按 `Session.isAdmin` 分流 `ALLOWED`/`DENIED_NOT_ADMIN`；失败态映射：`Unauthorized`→`SESSION_EXPIRED`，`Forbidden`/`AdminAccessDenied`→`DENIED_NOT_ADMIN`，`InvalidInstance`→`DENIED_GUEST`，其余→`ERROR`。真正意外异常（非六态覆盖范围）才允许 `ApiResult.Failure` 冒泡，六态本身全部编码为 `ApiResult.Success(<state>)`（接口签名的返回值语义在实现处用注释记录了理由）。
- `observeAccess` 改用 `AuthRepository.observeSession` + `scan`（不额外发请求，纯本地响应式视图），seed 为 `CHECKING`；若上一状态是 ALLOWED/DENIED_NOT_ADMIN 而 session 变为 null/guest，判定为 `SESSION_EXPIRED` 而非 `DENIED_GUEST`（区分"本来有真实会话后失效"vs"从来没有"）。
- 单测 `AdminGateRepositoryImplTest`（13 个用例，MockK + `runTest`，风格照抄 `ExternalOpenRepositoryImplTest`）：`checkAccess` 覆盖 无本地会话/游客会话→DENIED_GUEST（2 例）、刷新成功+admin→ALLOWED、刷新成功+非admin→DENIED_NOT_ADMIN、刷新失败 Unauthorized→SESSION_EXPIRED、Forbidden→DENIED_NOT_ADMIN、其他错误→ERROR、InvalidInstance→DENIED_GUEST、意外异常→Failure（共 8 例）；`observeAccess` 覆盖 CHECKING→ALLOWED、CHECKING→DENIED_NOT_ADMIN、CHECKING→DENIED_GUEST（无会话）、CHECKING→ALLOWED→SESSION_EXPIRED（会话中途失效）（共 4 例，另加 1 例 seed 验证并入上述）。全部通过。

### S2-T2 设置页「管理台」入口行
- `SettingsViewModel`：新增 `AdminEntryState`（`HIDDEN`/`DISABLED_NOT_ADMIN`/`ENABLED`，纯展示层枚举，明确 KDoc 说明不等同于 `AdminAccessState` 且不调用 `AdminGateRepository`）；`adminEntryState: StateFlow` 用 `flatMapLatest` 跟随当前实例切换观察 `AuthRepository.observeSession`；新增 `currentInstanceName: StateFlow<String?>` 供入口副标题使用。
- `SettingsScreen.kt`：`currentInstanceId?.let{...}`（任务中心行）之后新增管理台行；`HIDDEN`→不渲染；`DISABLED_NOT_ADMIN`→渲染但 `enabled=false`，副标题「{实例名} · 需要管理员权限」（PRD §8.2.1「可看到管理台入口不可用说明」——不隐藏，与 §9.3 "D-02.4" 一致）；`ENABLED`→正常行，副标题为实例名，点击 `onOpenAdmin(instanceId)`。私有 `SettingsRow` 增加 `enabled: Boolean = true` 参数（`clickable(enabled=...)` + 文字变灰）。
- `OpenListNavHost.kt`：`SettingsScreen` 调用点新增 `onOpenAdmin = { instanceId -> navController.navigate(Routes.admin(instanceId)) }`。

### S2-T3 ADMIN 宿主页 + 门控页 + AdminScaffold/AdminTabRow
- `core:designsystem` 新增 `AdminComponents.kt`/`AdminTabRow`：`TaskTabRow` 的横向可滚动变体（`Row` + `horizontalScroll`，每个 Tab 按内容宽度而非等宽），沿用同款下划线激活态视觉。
- `feature:admin` 新增 `AdminViewModel`（Hilt，`SavedStateHandle` 读 `instanceId`/`tab`）：`init` 中硬性调用一次 `AdminGateRepository.checkAccess`（PRD"进入 ADMIN 路由即 checkAccess"的硬要求），同时 `launchIn` 收集 `observeAccess` 以响应式跟踪后续失效（两路径都写同一个 `accessState` 字段，谁后完成以谁为准，最终都会收敛到 session Flow 反映的真实状态，无需额外协调逻辑）。
- `AdminHostScreen.kt` 重写：`when(accessState)` 分支——`CHECKING`→`LoadingState`；`SESSION_EXPIRED`→`LaunchedEffect` 触发 `onSessionExpired(instanceId)`（复用现有 `Routes.login` + `popUpTo(0)` 登录流程，未新造机制）；`DENIED_GUEST`/`DENIED_NOT_ADMIN`/`ERROR`→`AdminAccessDeniedScreen`（`EmptyState` 三种文案）——这三个分支的 `when` 结构本身保证零 Tab 组件被调用（不是运行时检查，是编译期的分支结构：`AdminOverviewTab`等 Tab 内容 Composable 只出现在 `ALLOWED` 分支里）；`ALLOWED`→`AdminScaffold`（顶部栏副标题显示当前实例名 + `AdminTabRow` 7 Tab + 选中 Tab 内容，非 Overview 的 6 个 Tab 渲染 `AdminComingSoon`「即将上线」占位）。
- `OpenListNavHost.kt`：`Routes.ADMIN` 调用点新增 `onSessionExpired = { id -> navController.navigate(Routes.login(id)) { popUpTo(0) } }`（与既有 `LOGIN` 路由 `onSwitchInstance`/`onAuthenticated` 同款 `popUpTo(0)` 重置栈手法一致，未新增机制）。

### S2-T4 概览 Tab
- 新增 `AdminOverviewTab.kt`：实例信息卡（`instanceInfo` 来自 `AdminViewModel.loadInstanceInfo`——`InstanceRepository.getById` + `AuthRepository.getSession` 两个本地零请求调用的组合，门控阶段已发生的 `/api/me` 数据不再重复请求）；存储/任务/索引三张摘要卡各自独立 `AdminCardState<T>`（`Loading`/`Loaded`/`Failed`），`AdminViewModel` 用三个独立 `viewModelScope.launch` 分别调用 stub 的 `AdminStorageRepository.getStorages`/`AdminTaskRepository.refreshUndone`/`AdminIndexRepository.getProgress`——三者全部返回 `ApiResult.Failure(DomainError.Unknown(null))`（S1 stub 未变），因此本 Sprint 三卡片均合法地展示"失败+重试"态，符合 PRD §16.3.4 独立降级要求（结构上互不阻塞：改任意一个 Repository 调用失败不影响另外两个的 `_uiState.update`）。
- Web 兜底入口卡：纯静态展示卡（标题+"即将上线，敬请期待"文案），未绑定 `AdminWebFallbackRepository`（该 Repository 仍是 S1 stub，真正接线在 S7）。

### 验证
`JAVA_HOME="C:/Program Files/Java/jdk-20" ./gradlew :feature:admin:compileDebugKotlin :app:compileDebugKotlin :data:repository:compileDebugKotlin :core:designsystem:compileDebugKotlin --console=plain` → **BUILD SUCCESSFUL**。`:data:repository:testDebugUnitTest` → **BUILD SUCCESSFUL**（`AdminGateRepositoryImplTest` 13/13 通过）。`git status` 确认改动范围仅限 `app/navigation`、`data/repository`（Admin相关）、`feature/admin`、`feature/settings`、`core/designsystem`（新增 `AdminComponents.kt`），无 `openlist-ref/` 暂存、无越界文件。

### 遗留给 S3+
1. `AdminViewModel.refreshTaskSummary` 目前只依据 `refreshUndone` 的成功/失败推导摘要，未真正统计 `observeAdminTasks` 的运行中任务数（`runningCount` 暂写死为 0）——S5 实现真实 `AdminTaskRepository` 后需要回来把这里接上真实计数。
2. Overview 卡片的"重试"目前只重跑单张卡片的 Repository 调用；S3+ 若发现三卡片需要联动刷新（如下拉刷新整页），需要在 `AdminViewModel` 增加一个统一入口，当前实现未预留。

---

## S3：用户查看 + 存储只读

### S3-T1 AdminUserRepositoryImpl
- `core:model` 新增 `kotlinx.serialization` 插件依赖（`core/model/build.gradle.kts`）——唯一原因：`AdminUserPage`/`AdminUserSummary`/`AdminStoragePage`/`AdminStorageSummary`/`AdminStorageDetails`/`AdminStorageStatus` 标注 `@Serializable`，供 `admin_cache.rawJson` 直接存领域模型 JSON（P-507）。`core:model` 本身不解码任何后端原始响应，序列化仅用于"领域模型 -> 缓存行"这一方向。
- `AdminUserRepositoryImpl`（`data/repository`）：`getUsers` 走标准 `instanceRepository.getById -> instanceContext.set -> clientFactory.apiFor -> safeApiCall -> 401 sessionManager.invalidate` 模式（照抄 `FilesRepositoryImpl.getFile`/`AuthRepositoryImpl.refreshCurrentUser` 的写法）；`admin_cache` 读写：scope="users"，`cacheKey="page:$page"`，TTL 1 分钟，`forceRefresh=true` 绕过缓存直接打网络并重新写缓存。`getUser` 不缓存（接口无 forceRefresh 参数，单用户查询走网络，决策见下）。
- `AdminUserDto.toDomain()`：`password` 字段读入 DTO 但从不复制进 `AdminUserSummary`（结构性不存在该字段，不是"脱敏"步骤）；`otp` 字段不采信为 `otpEnabled`（S1-T3/T4 已记录：源码未确认 2FA 状态字段），映射结果固定为 `null`（"未知"）而非猜测 `false`。`roleLabel` 按 `Session.ROLE_ADMIN/GUEST/GENERAL` 常量本地推导中文文案。

### S3-T2 用户 Tab UI
- 新增 `AdminUserListViewModel`（`feature/admin`）：`bind(instanceId)` 幂等绑定；`refresh()` 走 `forceRefresh=true`（下拉刷新语义）；简单的"加载更多"分页（无滚动触发，无项目内既有分页范式可循，见下方决策）。
- 新增 `AdminUserTab.kt`：`PullToRefreshBox` + `LazyColumn`（三态走 `LoadingState`/`EmptyState`/`ErrorBar`，与 `ShareListScreen` 同款结构）；行展示用户名/角色徽标（管理员=`StatusTone.PRIMARY`）/禁用徽标/基础路径+权限位摘要；点击行打开只读 `ModalBottomSheet`（`AdminUserDetailSheet`），仅展示 `AdminUserSummary` 的字段，无任何编辑入口。
- 接入 `AdminHostScreen.kt` 的 `AdminScaffold`：`AdminTab.USERS` 分支渲染 `AdminUserTab(instanceId = instanceId)`（`instanceId` 从 `AdminHostScreen` 参数直接下发，未经 `uiState`，因为 `AdminUiState` 本身不携带 instanceId）。

### S3-T3 AdminStorageRepositoryImpl（只读部分）
- `getStorages`/`getStorage` 同款标准模式；`admin_cache` scope="storages"，`cacheKey="list"`，TTL 30 秒。enable/disable/reloadAll/driver 三组方法保持 S1 stub（`ApiResult.Failure(DomainError.Unknown(null))`），按 brief 留给 S4——本 Sprint **未**顺带接入驱动只读端点（`getDrivers`/`getDriverNames`/`getDriverInfo` 虽是无副作用 GET，但把它们和 enable/disable 拆开更符合"S3 只做只读列表/详情"的单一 Sprint 边界，留给 S4 一并做，S4 brief 会复核）。
- `AdminStorageDto.toDomain()`：`mountDetails` 为 null/absent 时 `mountDetails` 字段直接为 `null`，不抛异常（DTO 字段本身可空，未解码到值时函数根本不调用 `.toDomain()` 扩展）；`status` 推导规则：`disabled=true` -> `DISABLED`；`status` 为空 -> `UNKNOWN`；`status=="work"`（大小写不敏感）-> `ENABLED`；其余非空字符串（多为 driver 错误信息）-> `ERROR`。

### S3-T4 存储 Tab UI（只读部分）
- 新增 `AdminStorageListViewModel`：`bind`/`refresh`（`forceRefresh=true`）/`selectStorage`/`dismissStorageDetail`。
- 新增 `AdminStorageTab.kt`：列表行（挂载路径/状态徽标/驱动+备注摘要）；详情 `ModalBottomSheet`：基础字段 + 独立的"挂载详情"子区块（`mountDetails==null` 时展示"详情不可用"文案，而非阻塞整个 Sheet 的加载态——`getStorage` 本身已一次性返回完整 `AdminStorageSummary`，无需二次网络请求）；启用/禁用按钮渲染为**禁用态视觉占位**（`OutlinedButton(enabled=false)`，无 `onClick` 副作用），文案提示"将在后续版本提供"，未接入任何真实调用（S4 范围）。
- 接入 `AdminHostScreen.kt`：`AdminTab.STORAGES` 分支渲染 `AdminStorageTab(instanceId = instanceId)`。

### 决策记录（brief 存在歧义处）
1. **`getUser` 不缓存单用户详情**：接口签名 `getUser(instanceId, id): ApiResult<AdminUserSummary>` 无 `forceRefresh` 参数，且单用户详情查询频率低（仅详情 Sheet 打开时触发一次），复杂度收益比不划算，故不接入 `admin_cache`（brief 原文允许"简单起见不缓存单用户查询"这一选项）。
2. **分页 UI 采用"加载更多"按钮而非滚动触发**：全项目搜索未发现任何滚动位置驱动分页的既有范式（files/share/task/search 全部一次性拉取整页数据），引入 `derivedStateOf`+`LazyListState` 滚动监听是本 Sprint 唯一的全新模式，风险/收益比不如一个简单的"加载更多" `TextButton`，两者都满足接口约定（`getUsers(page, forceRefresh)`），选择更小的改动面。
3. **只读详情 BottomSheet 无既有范式可抄**：项目现有全部 `ModalBottomSheet` 用例都是操作菜单/表单/选择器（`FileActionSheet`/`ShareFormSheet`/`OfflineDownloadSheet`/`DirectoryPickerSheet`），没有纯只读详情的先例；`AdminUserDetailSheet`/`AdminStorageDetailSheet` 照抄 `FileActionSheet` 的 `ModalBottomSheet` 骨架（`rememberModalBottomSheetState`/`onDismissRequest`），内容区改为纯 `Text`/`Row`（无 `clickable`），是本 Sprint 唯一的新组件形态但结构简单、无风险。
4. **存储 status 推导规则的具体阈值**（"work"字符串、大小写不敏感）：V-503 已确认 `status` 是自由格式字符串（健康时常为"work"，否则为 driver 报错信息），因此选择"disabled 优先、work 判定健康、其余非空字符串一律 ERROR"作为保守判定——UNKNOWN 只保留给空字符串这一种情况，真机验证后如与实际驱动行为不符，留给 S8 校准。

### 偏差
未发现需要停下汇报的架构/范围级偏差。驱动只读端点（`getDrivers`/`getDriverNames`/`getDriverInfo`）本可在本 Sprint 顺带实现（brief 允许"你的判断"），但选择不做，理由见上方决策记录第 1 条——留给 S4 统一处理，届时会与 enable/disable 一起接线并测试。

### 单测
`AdminUserRepositoryImplTest`（6 例）：DTO 映射对非空 password 输入不崩溃且不泄露（结构性断言 `AdminUserSummary` 反射字段名不含 password/pwd/salt/secret）、`getUser` 端到端不泄露 password、`getUsers` TTL 内命中缓存零网络调用、`forceRefresh` 恒打网络、缓存过期（2 分钟前，TTL 1 分钟）触发刷新、401 触发 `sessionManager.invalidate` 并透传失败。
`AdminStorageRepositoryImplTest`（9 例）：`mountDetails` 为 null 时映射仍可用、`mountDetails` 存在时正确解码磁盘用量、disabled 优先于 status 字符串判定为 DISABLED、非"work"字符串判定为 ERROR、`getStorages` TTL 内命中缓存零网络调用、`forceRefresh` 恒打网络、缓存过期（31 秒前，TTL 30 秒）触发刷新、401 触发 `sessionManager.invalidate` 并透传失败、`getStorage` 单条查询在 `mountDetails` 缺失时仍映射成功。
全部 15 例通过（`:data:repository:testDebugUnitTest` BUILD SUCCESSFUL）。

### admin_cache.rawJson 只含领域模型 JSON 的核实
`AdminUserRepositoryImpl.getUsers`/`AdminStorageRepositoryImpl.getStorages` 写缓存时调用 `json.encodeToString(domain)`，`domain` 的类型分别是 `AdminUserPage`/`AdminStoragePage`（`core:model`，`@Serializable`，S3-T1 新增标注）——序列化对象是 DTO->domain 映射**之后**的结果，此时 `password` 已经不在对象图里（`AdminUserSummary` 没有该字段），`AdminUserDto`/`AdminStorageDto`（`core:network`，未标注可从 `data:repository`/`core:model` 访问的语境内被序列化）从未被传给 `json.encodeToString`。核实方式：`data:repository` 源码里唯一调用 `adminCacheDao.upsert` 的两处（`AdminUserRepositoryImpl.getUsers`/`AdminStorageRepositoryImpl.getStorages`）传入的 `rawJson` 参数均为 `json.encodeToString(domain)`，`domain` 变量在两处都是刚由 `result.data.content.map { it.toDomain() }` 构造出的领域模型列表包装对象，DTO 类型（`AdminUserPageDto`/`AdminStoragePageDto`）本身没有被标注为可从这条路径序列化，也没有任何 `encodeToString<AdminUserDto>`/`encodeToString<AdminStorageDto>` 调用点。

### 验证
```
JAVA_HOME="C:/Program Files/Java/jdk-20" ./gradlew :feature:admin:compileDebugKotlin :app:compileDebugKotlin :data:repository:compileDebugKotlin :core:designsystem:compileDebugKotlin --console=plain
```
BUILD SUCCESSFUL（305 actionable tasks，38 executed）。
```
JAVA_HOME="C:/Program Files/Java/jdk-20" ./gradlew :data:repository:testDebugUnitTest --console=plain
```
BUILD SUCCESSFUL（`AdminUserRepositoryImplTest` 6/6、`AdminStorageRepositoryImplTest` 9/9，加上既有 `AdminGateRepositoryImplTest` 13/13 等全部既有用例均未回归)。`git status` 确认改动范围仅限 `core/model`（`AdminModels.kt` 加 `@Serializable`、`build.gradle.kts` 加序列化插件）、`data/repository`（两个 Impl + 两个新测试文件）、`feature/admin`（两个新 ViewModel + 两个新 Tab + `AdminHostScreen.kt` 接线），无 `openlist-ref/` 暂存、无越界文件。

### 遗留给 S4+
1. 驱动列表/名称/信息只读端点（`AdminStorageRepository.getDrivers/getDriverNames/getDriverInfo`）仍是 S1 stub，未在 S3 顺带实现——留给 S4 与 enable/disable/load_all 一并接线（决策记录第 1 条）。
2. 存储 Tab 详情 Sheet 已渲染禁用态的启用/禁用按钮占位（无 `onClick`），S4 需要：(a) 把它们接上 `AdminStorageRepository.enableStorage/disableStorage`，(b) 补二次确认弹窗（危险样式用于禁用），(c) 成功后按 §10.3 触发文件/预览缓存联动并刷新列表缓存。当前占位没有为这些操作预留回调参数，S4 实现时需要扩展 `AdminStorageTab`/`AdminStorageListViewModel` 的参数面。
3. 存储 Tab 顶部尚未渲染"重新加载全部存储"入口（S4 范围）。
4. 用户 Tab 分页用简单"加载更多"按钮而非滚动触发，如后续 Sprint 引入统一的滚动分页范式，需要回来同步这里。
5. `AdminUserDto.otp`/`AdminUserSummary.otpEnabled` 仍是 provisional（S1 遗留项 1 未变，S3 详情 Sheet 展示为"未知"而非猜测值）。

---

## S4：存储启停 + 驱动信息 + 缓存联动

### S4-T1/T2 AdminStorageRepositoryImpl：enable/disable/reload-all + 缓存联动
- `data/repository/AdminStorageRepositoryImpl.kt`：`enableStorage`/`disableStorage` 走共享的 `setStorageEnabled`（标准 instance-lookup -> instanceContext.set -> clientFactory.apiFor 模式，与 S3 的 `getStorages` 一致）；`reloadAllStorages` 独立实现。
- **缓存失效机制**：选择"删除 `admin_cache` 对应 scope 的行"（`adminCacheDao.deleteByScope(instanceId, "storages")`），而非"把 `cachedAt` 前移使其过期"——两者等价，前者更简单（brief 允许两选一）。
- **精确 vs 保守失效**（PRD §13.3）：enable/disable 成功后，二次调用 `api.adminStorageGet(id)` 取回该 storage 的 `mountPath`（不依赖 UI 传入的可能过期值），`OpenListPathCodec.normalize()` 归一化尾斜杠后调用 `fileCacheDao.deleteByPathPrefix`/`previewRepository.invalidateByPrefix`；reloadAll 成功后做保守广谱失效：`fileCacheDao.deleteByInstanceId` + `previewRepository.invalidateByPrefix(instanceId, "/")`（`PreviewRepository` 没有 `invalidateAll(instanceId)` 形态的方法，"/" 是最广前缀，语义上等价于清空该实例全部预览缓存）。
- 失败路径：完全不触碰任何缓存/DAO 调用（`if (result is ApiResult.Success) {...} else { onUnauthorized(...) }`），天然满足"失败保留原状态"（PRD §13.2.5），验证方式见下方单测列表。
- **跨 Repository 依赖**：构造函数新增 `fileCacheDao: FileCacheDao`/`previewRepository: PreviewRepository`，KDoc 引用 v0.4 `FileOperationRepositoryImpl` 建立的"Repository 依赖另一个 Repository/DAO 仅用于缓存失效副作用"先例，明确这个依赖只用于 enable/disable/reloadAll 三个成功路径，不用于本 Repository 的读路径。
- **错误信息透传**：发现共享的 `safeApiCallUnit`（经 `ErrorMapping.kt` 的 `codeToDomainError`）把所有 500 状态码统一收敛成通用 `DomainError.ServerError`（无 message），而后端 enable/disable/load_all 失败**固定是 HTTP 500 + 具体 Go error message**（如 "this storage have enabled"/"failed get storage driver"，源码 `openlist-ref/internal/op/storage.go:171-216`）——PRD §13.2.5/§14.2 要求原样展示后端 message，与 `ServerError` 桶化冲突。新增私有 `safeAdminOperationCall`（不改动共享 `ErrorMapping.kt`，只在这 3 个写端点内联使用）：401 仍映射 `Unauthorized`（配合既有 `onUnauthorized`/`sessionManager.invalidate` 分支），其余非 2xx 一律 `DomainError.OpenListError(code, message)` 透传原文，符合 v0.5_EXECUTION_PLAN.md §5.1/P-510 "操作类失败经 OpenListError 透传 message" 的既定决策。
- `getDrivers`/`getDriverNames`/`getDriverInfo`：简单 GET，复用共享 `safeApiCall`，**不接入 `admin_cache`**（brief 允许，驱动信息访问频率低，复杂度收益比不划算）。`getDrivers`/`getDriverInfo` 的 `AdminDriverInfoMap`（`Map<String, JsonElement>`）与接口签名 `Map<String, Any?>` 恰好类型兼容（`JsonElement : Any?`），无需额外转换层。

### S4-T3 存储 Tab 确认弹窗 + 操作接线
- `feature/admin/AdminStorageListViewModel.kt`：新增 `AdminStorageDialog`（`EnableConfirm`/`DisableConfirm`/`ReloadAllConfirm`）+ `dialogLoading`/`dialogError`/`snackbarMessage` 字段（结构照抄 `feature/files/FileListViewModel.kt` 的 `FileListDialog`/`dialogLoading`/`dialogError` 模式——该文件是全项目里"二次确认弹窗 + loading/error 内联展示"的既有范式来源）。`confirmDialog()` 分发到 `setEnabled`/`reloadAll`；成功后：dismiss 弹窗 + 设置 snackbarMessage + `refresh()`（forceRefresh 强制刷新列表)+ `refreshSelectedDetail(id)`（若详情 Sheet 仍开着同一 storage，二次拉取 `getStorage` 刷新详情，不做整页刷新）；失败：`dialogLoading=false, dialogError=<message>`，弹窗保持打开且不触碰其余 UI 状态。
- **重新加载全部存储的确认弹窗样式决策**：选择**非危险样式**。依据：PRD §8.5 第 511 行原文"清空索引、禁用存储应使用危险操作样式"仅列出这两项；§12.4 存储页操作表格里"重新加载全部存储"标注"二次确认"而非"危险确认"（与"禁用存储"行的"危险确认"字面区分开）；EXECUTION_PLAN §10.5.5 摘要同样只点名 disable-storage/clear-index 为危险样式。三处证据一致指向"reload-all 是普通二次确认"，按 brief "存疑时偏向普通样式"的指导采用非危险 `ConfirmDialog(danger=false)`（默认值）。
- `feature/admin/AdminStorageTab.kt`：列表顶部新增可点击的"重新加载全部存储"行（`ReloadAllRow`，位于 `LazyColumn` 的第一个 `item{}`，PRD §12.4.7 要求的入口位置）；详情 Sheet 的启用/禁用按钮从 S3 的"视觉禁用占位"改为真实可点击（`Button`/`OutlinedButton`，`onClick` 触发 `viewModel.requestEnable/requestDisable`，真正的网络调用发生在确认弹窗的 `onConfirm`），按钮文案/组件类型随 `storage.disabled` 切换（启用=`Button`主操作，禁用=`OutlinedButton`次操作，两者都不在列表行本身而在详情 Sheet 里，与 brief "详情 Sheet 承载操作入口，更简单"的建议一致）；`Scaffold(snackbarHost=...)` + `SnackbarHostState` 展示操作结果文案（成功/失败均走 snackbar，弹窗关闭后展示；reload-all 成功文案固定为"重新加载已提交，正在后台进行"，绝不使用"已完成"措辞，对应 PRD §13.2.2 的异步语义）。

### S4-T4 驱动信息 Sheet + 概览摘要卡点亮
- **驱动信息入口位置决策**：放在存储详情 Sheet 内的"查看驱动信息"行（点击后打开**第二个、独立的** `ModalBottomSheet`，`AdminDriverInfoSheet`），而非列表行或单独 Tab 内子导航——理由：驱动信息是单个存储的关联参考数据，跟随该 storage 的详情上下文最自然，且避免在同一个 Sheet 内堆叠过多状态（`AdminDriverInfoUiState` 独立于 `selectedStorage`，两个 Sheet 可分别 dismiss，不互相阻塞）。
- `AdminDriverInfoSheet` 内容：`AdminCardState<Map<String,Any?>>` 三态（Loading/Loaded/Failed，复用 `AdminViewModel.kt` 已有的 `AdminCardState` sealed class，未重新发明一套）；`Loaded` 分支把 map 的每个 key/value 渲染成 `DetailRow`（value 用 `.toString()`，`JsonElement` 的 `toString()` 已经是可读的 JSON 文本），未做任何结构化解析/类型判断，符合 PRD "结构复杂时只展示驱动名+关键摘要" 的兜底要求。
- **概览存储摘要卡点亮**：核实 `AdminViewModel.refreshStorageSummary()`（S2 就已写好调用 `adminStorageRepository.getStorages(instanceId)` 并统计 enabled/disabled 计数）——该函数在 S3 把 `getStorages` 从 stub 换成真实实现后就已经自动点亮，本 Sprint **无需改动** `AdminViewModel.kt`；核实方式：读代码确认调用路径未变，`git diff` 显示本 Sprint确实未触碰该文件。

### 单测（`data/repository/src/test/.../AdminStorageRepositoryImplTest.kt`，新增 13 例，累计 18 例）
1. `enableStorage success invalidates storages cache scope and precisely invalidates the mount path`（mountPath 含尾斜杠 `/nas/`，断言归一化后以 `/nas` 调用 `deleteByPathPrefix`/`invalidateByPrefix`）。
2. `disableStorage success invalidates storages cache scope and precisely invalidates the mount path`。
3. `enableStorage failure leaves cache untouched and surfaces the backend message`（500 + "this storage have enabled" → `OpenListError.message` 原样透传；`adminCacheDao`/`fileCacheDao`/`previewRepository`/二次 `adminStorageGet` 均零调用）。
4. `disableStorage failure leaves cache untouched and surfaces the backend message`（500 + "failed get storage driver"）。
5. `enableStorage 401 invalidates the session and does not touch caches`。
6. `reloadAllStorages success invalidates storages cache scope and broadly invalidates the whole instance`（断言 `deleteByInstanceId` + `invalidateByPrefix(instanceId, "/")`，且从不调用精确前缀版本 `deleteByPathPrefix`）。
7. `reloadAllStorages failure leaves cache untouched and surfaces the backend message`。
8. `getDriverNames returns the plain string list on success`。
9. `getDriverInfo 404 (unknown driver) surfaces as a Failure, not a crash`。
全部通过（`:data:repository:testDebugUnitTest` BUILD SUCCESSFUL，本文件 18/18，模块累计 100/100，无回归）。

### 失败保留原状态的核实方式
每个 failure 分支单测显式 `coVerify(exactly = 0)` 断言 `adminCacheDao.deleteByScope`/`fileCacheDao.deleteByPathPrefix`/`fileCacheDao.deleteByInstanceId`/`previewRepository.invalidateByPrefix` 均未被调用；ViewModel 层失败路径只更新 `dialogLoading`/`dialogError`，`storages`/`selectedStorage` 字段保持不变（读代码确认 `is ApiResult.Failure -> _uiState.update { it.copy(dialogLoading = false, dialogError = ...) }` 分支不触碰其余字段）。

### 决策记录（brief 存在歧义处）
1. 缓存失效机制：删除缓存行（`deleteByScope`）而非重写 `cachedAt`——见上方 S4-T1/T2。
2. reload-all 确认弹窗样式：非危险——见上方 S4-T3，三处 PRD/计划证据交叉核实。
3. 驱动信息入口位置：存储详情 Sheet 内的二级 Sheet——见上方 S4-T4。
4. enable/disable 失败时的错误信息透传机制：新增 scoped 的 `safeAdminOperationCall`（不改动共享 `ErrorMapping.kt`）——见上方 S4-T1/T2，这是本 Sprint 唯一超出"复用现有 safeApiCall/safeApiCallUnit"预期的实现决定，原因是共享映射会丢失 v0.5 admin 操作类接口需要原样透传的后端 message。

### 偏差
无需停下汇报的架构/范围级偏差。`safeAdminOperationCall` 的新增略微超出 brief"标准 safeApiCall 模式"的字面描述，但方向与 v0.5_EXECUTION_PLAN.md §5.1/P-510 的既定"操作类失败经 OpenListError 透传 message"决策完全一致，且改动范围收敛在 `AdminStorageRepositoryImpl.kt` 内部，未触碰共享 `core:network` 的 `ErrorMapping.kt`，不影响其他模块。

### 验证
```
JAVA_HOME="C:/Program Files/Java/jdk-20" ./gradlew :feature:admin:compileDebugKotlin :app:compileDebugKotlin :data:repository:compileDebugKotlin :core:designsystem:compileDebugKotlin --console=plain
```
BUILD SUCCESSFUL（305 actionable tasks）。
```
JAVA_HOME="C:/Program Files/Java/jdk-20" ./gradlew :data:repository:testDebugUnitTest --console=plain
```
BUILD SUCCESSFUL（模块累计 100/100 测试通过，`AdminStorageRepositoryImplTest` 18/18，无回归）。`git status` 确认改动范围仅限 4 个文件：`data/repository/AdminStorageRepositoryImpl.kt`、`data/repository/.../AdminStorageRepositoryImplTest.kt`、`feature/admin/AdminStorageListViewModel.kt`、`feature/admin/AdminStorageTab.kt`，无 `openlist-ref/` 暂存、无越界文件。

### 遗留给 S5+
1. 无存储相关未完成项——enable/disable/reload-all/驱动信息只读/概览存储摘要卡点亮均已实现并验证。
2. S3 遗留项 5（`AdminUserDto.otp`/`AdminUserSummary.otpEnabled` provisional）与本 Sprint 无关，继续保持"未知"展示，等待真机核实。
3. 未发现新的跨 Sprint 遗留问题。

---

