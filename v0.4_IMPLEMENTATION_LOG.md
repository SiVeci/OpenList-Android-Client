# v0.4 实施日志

> 本文件按 Sprint 追加记录：完成任务、文件变更摘要、实现决定、偏差、V-40x 验证结论。体例配合 `v0.4_EXECUTION_PLAN.md` §11。

---

## S0：验证前置

### DEC-1 结论
`openlist-ref/`（后端参考源码的精简 checkout：`common/`、`handles/`、`middlewares/`、`model/`、`router.go`，31 个 Go 文件，已 gitignore）在仓库中已存在且可用，无需重新克隆。按 DEC-1 走"源码优先"路径，未降级为纯 API 文档兜底。

### V-401 `/api/fs/get` 直链字段
`raw_url` 由 `FsGet` 处理器（`fsread.go`）动态生成：需代理的存储返回 `/p{path}?sign=xxx`（`GenerateDownProxyURL`）；支持直链的存储返回驱动直链（`fs.Link()`/`model.GetUrl()`）。签名仅在存储启用签名/`SignAll` 时按路径生成，重新 `fs/get` 会拿到新签名。签名生成/校验细节（`internal/sign` 包）不在本地 checkout 内，标记 **provisional**。
**客户端结论**：沿用"每次进预览重新 fs/get，不复用旧 URL"的策略（S2 落地）。

### V-402 `/d/*path` 与 `/p/*path`
`/d/*path`：302 重定向到驱动直链（`down.go` `Down()`）。`/p/*path`：服务端反代/流式（`down.go` `Proxy()`）。`raw_url` 优先级复核确认：`fs/get` 内部按 `MustProxy()`/驱动能力决定返回代理路径还是直链，客户端固定"优先用 `raw_url`，为空时才退回 `/d/`+sign"的既有 v0.1 结论成立。`/p/` 代理路径确认是部分驱动（`MustProxy()`/`WebProxy`）的必需路径。

### V-403 Range 支持
`/d/*path` 是 302 重定向，Range 支持取决于目标驱动直链本身。`/p/*path` 反代路径下，`common.ProxyRange()` 按存储配置 `ProxyRange` 布尔值有条件转发 Range 头；该配置为驱动/存储级别，客户端无法预知。**客户端结论**：不假设 Range 一定可用；播放器按 P-412 兜底策略处理，不对 Range 失败做特殊报错，只统一走"播放失败→刷新→外部打开/下载"链路。

### V-404（新增确认，非计划原表格项，但与 V-410 相关）
鉴权失败统一返回 HTTP 状态码（401/403）+ JSON body（`common.ErrorResp`/`ErrorStrResp`），不是重定向或 200+错误体。

### V-405 分享详情已知路径
`sharing.go`：分享详情文件路径导出为分享态 URL（`/sd/{sid}/*path`），**不能**直接用创建者的普通 `fs/get` 访问同一分享态路径字符串；但 `Share.files[]` 在 v0.3 客户端模型中存的是原始文件系统路径（非分享态 URL），创建者本人对该原始路径走普通 `fs/get` 是可行的（权限检查按 `user.BasePath` 前缀匹配）。**客户端结论**：P-406 维持"仅登录用户、以自身权限走普通 fs 链路"不变，分享详情预览入口传入的是原始路径而非分享 URL。

### V-406 字幕候选可靠性
`fs/list`→`ObjResp[]` 含 `name`/`size`/`modified`/`type` 等完整字段，且后端自身已有 `filterRelated()`（`fsread.go`，按去扩展名前缀匹配）证明这套匹配思路成立。**客户端结论**：P-414 的"同目录 fs/list + 文件名前缀匹配"方案确认可靠，源码确认级别。

### V-407 路径编码一致性
逻辑路径（JSON body 中的 `path` 字段）纯文本不编码；下载/代理 URL 的路径段编码由客户端（`OpenListPathCodec`/OkHttp `addPathSegment`）负责，服务端生成 `/p` URL 时用 `utils.EncodePath()` 同规则编码。两端约定一致，v0.1 起的 `OpenListPathCodec` 封装继续适用，无需新增编码分支。

### V-408 / V-409 / V-410
- V-408（Media3 容器支持面）：纯客户端能力问题，不依赖后端源码，S5 实现时按默认 ExoPlayer 解码器能力处理，不支持的格式走 `MediaUnsupported`。
- V-409（Markdown 相对资源解析）：客户端设计问题（`basePath` + `/d/` 构造），S3 实现。
- V-410（签名/Token 失效表现）：`middlewares/auth.go` 确认 Token 失效场景（过期/篡改/密码变更/被禁用/被 invalidate）统一返回 401 JSON；签名验证中间件（`sign.Verify`）具体实现不在本地 checkout 内，**provisional**：客户端按"任何 4xx 播放/预览错误触发一次刷新"处理（P-412），不区分 401/403 细分因果。

### 结论
本地 `openlist-ref` 精简副本覆盖了 fs/get、fs/list、down、sharing、auth 中间件的路由/处理器层逻辑，足以支撑 S0 所需验证结论定稿；`internal/sign`、`server/common`（ProxyRange/Proxy 实现）等内部包不在 checkout 范围内，相关细节标记 provisional，按 DEC-1 兜底口径（防御式解码、安全默认值、不假设成功）处理，不阻塞实现。

### DEC-2/DEC-3
按用户预授权直接采纳：Markdown 渲染库 = Markwon（core 最小引入）；引入 androidx.media3（exoplayer + ui，随实现定版）。版本目录变更将在 S1 落地并在此记录。

---

## S1：基座

### 完成任务
S1-T1~T5 全部完成。

### 文件变更摘要
- **新增模块** `:feature:preview`（`feature/preview/build.gradle.kts` + 占位 Composable `PreviewPlaceholderScreen`/`MediaPlayerPlaceholderScreen`），依赖 `core:{common,model,domain,designsystem}` + Coil/Media3/Markwon，不依赖任何 `feature:*`/`data:repository`，依赖方向核查通过、无环。
- 删除 `app/.../feature/preview/Reserved.kt` 占位包；`app/build.gradle.kts` 加 `:feature:preview` 依赖、移除未使用的 `libs.coil.compose`（下放至 `:feature:preview`）。
- **Room 7→8**：新增 `PreviewCacheEntity`（`id/instanceId/path/kind/mimeType/lastModified/cacheKey/localFilePath/sizeBytes/etag/expiresAt/cachedAt` + `(instanceId,path,kind)` 唯一索引）、`PreviewCacheDao`，`OpenListDatabase` 升至 version 8，手写 `MIGRATION_7_8`，手工推导 `8.json`（**未经 KSP 实际导出验证**，identityHash 为占位值，字段已逐一三方核对一致，见下方核对表）。`InstanceRepositoryImpl.delete` 级联清理 `previewCacheDao.deleteByInstanceId` + 删除 `cacheDir/preview/<instanceId>/` 目录。
- **领域模型**（`:core:model`）：`PreviewTarget`/`PreviewKind`/`PreviewOpenMode`/`PreviewSource`(sealed, 目前仅 `RemoteUrl`)/`PreviewFallback`/`TextPreviewContent`/`MarkdownPreviewContent`/`TextPreviewOptions`/`PreviewUrl`/`MediaSource`(仅内存态)/`SubtitleCandidate`/`SubtitleSourceType`/`SubtitleSource`/`ExternalOpenTarget`；`PreviewKindResolver`(扩展名→PreviewKind 纯函数) + 单测（含大小写不敏感、无扩展名、以及修正后的 `.ts`→VIDEO 回归用例）。
- **Repository**：`:core:domain` 新增 `PreviewRepository`/`MediaRepository`/`SubtitleRepository`/`ExternalOpenRepository` 接口；`:data:repository` 对应 4 个空 Impl（方法体占位返回 `ApiResult.Failure(DomainError.Unknown(null))`，S2/S3/S5/S6 replace）；`RepositoryModule` 新增 4 个 `@Binds`。`DomainError` 新增 `PreviewTooLarge`/`MediaUnsupported`/`MediaSourceExpired`/`ExternalOpenUnavailable` + 文案。
- **依赖与路由**：`libs.versions.toml` 新增 `media3=1.4.1`（exoplayer+ui）、`markwon=4.6.2`（core）；`Routes.kt` 新增 `PREVIEW`/`MEDIA_PLAYER` 路由常量+builder；`OpenListNavHost.kt` 挂载两条路由骨架指向占位页。

### 实现决定
- `PreviewCacheEntity` 用合成 `id` 主键 + `(instanceId,path,kind)` 唯一索引（而非复合主键），便于 REPLACE 式 upsert；`cacheKey` 作为预拼接的查找字符串保留。
- `PreviewSource` 目前只有 `RemoteUrl(url, headersRequired)` 一个变体，按最小化设计，S2/S3 需要本地文件变体时再加。
- Repository 空实现用无参构造函数（S2 起按需加真实依赖），避免过早引入未使用的注入参数。
- Media3 1.4.1 / Markwon 4.6.2 为已知稳定版本，因无法联网核实"当前最新稳定版"，构建阶段如需升级另行处理。

### 与计划的偏差
- 无实质性偏差。首轮实现遗漏了计划 §8.2/§11.4 明确要求的 `mimeType`/`lastModified` 两个字段，且 `PreviewKindResolver` 首版把 `"ts"` 同时归入 TEXT 和 VIDEO（`.ts` 视频文件会被误判为文本）——均已发现并修正，详见下方核对表与单测。

### Room schema 三方核对表（Entity｜MIGRATION_7_8 SQL｜8.json，逐字段一致）
| 字段 | Entity | SQL | 8.json |
|---|---|---|---|
| id | String(PK) | TEXT NOT NULL, PK | ✓ |
| instanceId | String | TEXT NOT NULL | ✓ |
| path | String | TEXT NOT NULL | ✓ |
| kind | String | TEXT NOT NULL | ✓ |
| mimeType | String? | TEXT | ✓ notNull=false |
| lastModified | Long? | INTEGER | ✓ notNull=false |
| cacheKey | String | TEXT NOT NULL | ✓ |
| localFilePath | String | TEXT NOT NULL | ✓ |
| sizeBytes | Long | INTEGER NOT NULL | ✓ |
| etag | String? | TEXT | ✓ notNull=false |
| expiresAt | Long? | INTEGER | ✓ notNull=false |
| cachedAt | Long | INTEGER NOT NULL | ✓ |
| 唯一索引 | `@Index([instanceId,path,kind])` | `CREATE UNIQUE INDEX` | ✓ |

**Provisional 标注**：`8.json` 的 `identityHash` 为占位值，SQL/字段为手工推导，未经 Room KSP 实际导出验证——需在可构建环境下重新导出比对，已记入本 Sprint 遗留项。

### 遗留问题/下一 Sprint 注意点
- S2 实现 `resolvePreview` 时需给 `PreviewRepositoryImpl` 补齐真实依赖（`InstanceRepository`/`OpenListClientFactory`/`PreviewCacheDao`/`Context`），复用 `FilesRepositoryImpl.getFile` 的 instanceContext 设置+401 失效处理模式。
- `PreviewKindResolver.resolve` 已可供 S2 调用做 kind 判定。

---

## S2：分发 + 图片预览（v0.4 首个可演示闭环）

### 完成任务
S2-T1~T4 全部完成。经抽查（`PreviewRepositoryImpl`/`PreviewScreen`/`ImagePreviewSurface`/`FileListViewModel`/`FileListScreen`/`FileDetailScreen`/`OpenListNavHost` 逐文件 diff 核对），实现与计划严丝合缝，未发现需要打回的问题。

### 文件变更摘要
- **`PreviewRepositoryImpl.resolvePreview`** 真实实现：注入 `InstanceRepository`/`OpenListClientFactory`/`InstanceContext`/`SessionManager`，完全复用 `FilesRepositoryImpl.getFile` 的 instanceContext 设置 + 401 失效处理模式；每次新鲜 `fs/get`（V-401，不做本地缓存）；`kind→openMode/fallbacks` 决策表：IMAGE/TEXT/MARKDOWN/VIDEO/AUDIO→对应 `IN_APP_*`+[DOWNLOAD,EXTERNAL_APP]，PDF/OFFICE→`EXTERNAL_APP`+[DOWNLOAD,WEB]，UNKNOWN→`UNSUPPORTED`+[DOWNLOAD,EXTERNAL_APP,WEB]；目录防御分支返回 `UNSUPPORTED` 不崩溃；`headersRequired` 按 V-402 结论固定 `false`（/d//p 靠 URL 里的 sign 而非 Authorization 头）。`loadText`/`loadMarkdown`/`refreshPreviewUrl` 仍是 S1 占位，留给 S3/S5。单测 `PreviewRepositoryImplTest`（11 条，覆盖全部 8 个 kind + raw_url 回退 + 401 + InvalidInstance + 目录防御分支）。
- **预览宿主页**：`PreviewViewModel`（Hilt，`SavedStateHandle` 读 instanceId/path，`resolvePreview()` 每次重新请求不做缓存旁路标志）+ `PreviewScreen`/`PreviewScaffold`（`feature/preview/.../PreviewScreen.kt`），替换了 `OpenListNavHost.kt` 里 `Routes.PREVIEW` 路由原先指向的 `PreviewPlaceholderScreen`（已删除该占位 Composable）。`PreviewOpenMode` 全部 9 个枚举值在 `PreviewContentByOpenMode` 的 `when` 里各有分支（`when` 无 `else`，编译期穷尽性保证不会漏分支）：IN_APP_IMAGE 走真实图片渲染；IN_APP_TEXT/IN_APP_MARKDOWN 是"即将上线"占位态（留给 S3 替换）；IN_APP_VIDEO/IN_APP_AUDIO 用 `LaunchedEffect` 直接前转到 `Routes.MEDIA_PLAYER`（播放器页仍是 S1 占位，留给 S5）；EXTERNAL_APP/DOWNLOAD/WEB/UNSUPPORTED 是统一的"暂不支持应用内查看"占位态（留给 S4 的 `ExternalOpenSheet`）。
- **图片预览**：`ImagePreviewSurface`（Coil `AsyncImage` + `ContentScale.Fit`），`buildPreviewCacheKey(instanceId, path, modifiedAt) = "$instanceId:$path:${modifiedAt ?: 0L}"`（R-409 落地，单测 `ImagePreviewSurfaceTest` 5 条锁定构成）；加载失败态显示 `ErrorBar` + 一个禁用态"下载（暂未接入）"按钮（真正下载/外部打开留给 S4）。
- **入口接线**：`FileListViewModel.onNodeClick` 新增 `onOpenFile` 参数，用 `PreviewKindResolver.resolve` 判断 IMAGE/VIDEO/AUDIO/TEXT/MARKDOWN 走新回调，PDF/OFFICE/UNKNOWN 与目录点击/批量选择模式行为完全不变（P-404）；`buildFileActions` 新增"预览"菜单项（仅可预览 kind 显示）。`FileDetailScreen` 新增按 kind 区分文案的主操作按钮（查看图片/播放视频/播放音频/查看文档），原"下载"降级为 `SecondaryButton`；PDF/OFFICE/UNKNOWN/目录保持"下载"为唯一主操作，未改变。`OpenListNavHost.kt`：`FILE_LIST`/`FILE_DETAIL` 两个路由块都接了 `onOpenFile = { path -> navigate(Routes.preview(instanceId, path)) }`（`FILE_DETAIL` 块补充了此前没有的 `backStackEntry.arguments` 提取）。

### 实现决定
- 目录防御分支：`resolvePreview` 遇到 `isDir=true` 返回 `UNSUPPORTED`+空 fallbacks，不崩溃（正常入口不会传目录路径进来，纯防御）。
- `buildPreviewCacheKey` 放在 `:feature:preview`（不在 `:core:model`），因为它是 Coil 缓存键这一 UI 层关注点，不是领域概念。
- 图片加载失败态的"下载"按钮本 Sprint 只做禁用占位，不接 `TransferRepository`，避免跨入 S4 范围。
- 详情页"下载"降级为次要按钮（可预览 kind 时），非可预览 kind 保持原样不变。

### 与计划的偏差
无实质偏差。

### 遗留问题/下一 Sprint 注意点
- **S3**：替换 `PreviewScreen.kt` 里 `IN_APP_TEXT`/`IN_APP_MARKDOWN` 分支（约 124-134 行）；`PreviewRepositoryImpl.loadText`/`loadMarkdown` 仍是占位待实现。
- **S5**：替换 `IN_APP_VIDEO`/`IN_APP_AUDIO` 分支背后的 `MediaPlayerPlaceholderScreen`（`feature/preview/.../PreviewFeature.kt`），无需改动转发逻辑本身。
- **S4**：替换 `EXTERNAL_APP`/`DOWNLOAD`/`WEB`/`UNSUPPORTED` 分支为真正的 `ExternalOpenSheet`；`ImagePreviewSurface` 失败态的禁用下载按钮也在此接入真实下载/外部打开。
- `PreviewSource` 仍只有 `RemoteUrl` 一个变体；`headersRequired` 硬编码 `false`，若 S4/S5 发现某驱动确实需要鉴权头，需要回头调整。

---

## S3：文本 + Markdown 预览

### 完成任务
S3-T1~T4 全部完成。经抽查（`PreviewRepositoryImpl`/`PreviewCacheDao`/`PreviewHttpClient`/`FileOperationRepositoryImpl`/`UploadWorker`/`MarkdownPreviewSurface`/`TextPreviewSurface`/`PreviewViewModel`/`PreviewScreen`/`ReadCappedTest`/`FileOperationRepositoryImplTest` 逐文件核对），实现严谨、关键安全结论（Markdown HTML/外链处理）经实际查阅 Markwon 4.6.2 源码验证而非猜测，未发现需要打回的问题。

### 文件变更摘要
- **流式限长通道**（S3-T1）：`:core:network` 新增 `PreviewHttpClient`（独立 OkHttpClient，无 `AuthInterceptor`，30s/30s 超时，仿 `UploadHttpClient`）。`PreviewRepositoryImpl.streamReadCapped` 复用 `UploadWorker.executeCancellable` 的 `suspendCancellableCoroutine`+`call.cancel()` 取消模式；核心分块读取/截断逻辑与 BOM+UTF-8 解码被拆成两个顶层 `internal` 纯函数 `readCapped`/`decodeUtf8StrippingBom`（脱离 OkHttp，可独立单测），单测 `ReadCappedTest`（9 条：未截断/恰好等于上限/超上限截断/跨多个 8KB 分块/空流 + BOM 剥离 3 种场景）。
- **`loadText`**（S3-T2）：fs/get 拿新鲜元信息 → 超硬上限（20MB）直接 `PreviewTooLarge` → 未强制刷新时查 `preview_cache` 命中（`lastModified`/`sizeBytes` 与新鲜元信息一致且未过期）→ 命中读本地文件，未命中流式读取（512KB 软上限）+ 落盘 + upsert 缓存行。`TextPreviewSurface`（可选中复制、截断提示条、"复制全部文本"按钮）接入 `PreviewScreen` 的 `IN_APP_TEXT` 分支。
- **`loadMarkdown`**（S3-T3）：逻辑同 `loadText`（kind="MARKDOWN"，`basePath` 用 `OpenListPathCodec.parent`），接口签名补充了 `forceRefresh: Boolean = false` 参数（S1 遗漏，本 Sprint 明确要求补上，不算偏差）。`MarkdownPreviewSurface` 用 Markwon 4.6.2 core（`AndroidView(TextView)` 桥接）渲染，渲染异常 `runCatching` 降级为源码文本。**安全结论（经查阅 Markwon 源码验证）**：① HTML——未加 `markwon-html` 插件，`CorePlugin` 注册 `HtmlBlock`/`HtmlInline` 节点类型但不注册对应 visitor，未被访问的节点不产生输出，HTML 被静默忽略（不执行不显示原文）；② 外链——`LinkResolverDef`（Markwon 默认）已自带 `try/catch(ActivityNotFoundException)`，点击外链走系统 `ACTION_VIEW`，天然满足 P-410 且不会崩溃；③ 图片——本 Sprint **未实现**内嵌图片渲染（需要 `markwon-image` 依赖+自研 Coil `AsyncDrawableLoader`，超出本 Sprint 合理工作量，留待后续版本），纯文本内容不受影响。
- **缓存 TTL/失效联动**（S3-T4）：`PreviewCacheDao` 新增 `deleteByPathPrefix`/`getByPathPrefix`/`deleteByInstanceAndPath`。`PreviewRepository` 接口新增 `invalidate(instanceId, path)`/`invalidateByPrefix(instanceId, pathPrefix)`（失败内部吞掉，不向上抛错），`PreviewRepositoryImpl` 实现为"先删本地文件再删 DB 行"。`FileOperationRepositoryImpl` 注入 `PreviewRepository`（**本仓库首次出现 Repository 依赖 Repository**，仅限于写操作成功后的缓存失效副作用场景，明确注释为不推荐在别处效仿的特例），在 rename/remove/move/copy 四处 `fileCacheDao.deleteByPathPrefix` 旁各加一行 `previewRepository.invalidateByPrefix`；`UploadWorker` 注入 `PreviewRepository`，上传覆盖成功后调精确 `invalidate`（不是 `invalidateByPrefix`，因为只有单个文件被覆盖）。

### 实现决定
- 二级大文本策略：软上限 512KB（截断+`isTruncated=true`+提示，视为成功）、硬上限 20MB（`fs/get` 报告的 `size` 超过直接 `PreviewTooLarge`，不发起网络读取）——解决了计划文字里"截断兜底"与"超限直接 PreviewTooLarge"两种表述的表面矛盾。
- 缓存文件命名：`SHA-256("$instanceId:$path:$kind")` 十六进制串，同时作为 `PreviewCacheEntity.id` 主键与磁盘文件名，`cacheKey` 列存同一个拼接前字符串——`upsert` 的 REPLACE 因此直接命中主键，不需要依赖唯一索引冲突。
- Markdown 内嵌图片渲染明确裁剪，记入遗留项，不算未完成。
- `FileOperationRepositoryImplTest` 补了 4 条 `invalidateByPrefix` 断言；`UploadWorker` 的 `invalidate` 调用因无既有测试文件，只做代码走查，未补单测（已记录，非隐瞒）。

### 与计划的偏差
除计划中已预先批准的 `loadMarkdown(forceRefresh)` 签名补充外，无其他偏差。

### 遗留问题/S4-S6 需要注意的点
- S4：`ImagePreviewSurface`/`TextPreviewSurface` 里的禁用态"下载"占位按钮，以及 `PreviewScreen` 的 `EXTERNAL_APP`/`DOWNLOAD`/`WEB`/`UNSUPPORTED` 分支，是 `ExternalOpenSheet` 的接入点。
- S5：`refreshPreviewUrl` 仍是 S1 占位；播放器页仍是占位。
- Markdown 内嵌图片渲染留待后续版本（非 v0.4 必做项，PRD 允许裁剪）。
- `UploadWorker` 的预览缓存失效调用暂无回归测试覆盖。

---
